
# Don't store auto-generated nonsense
.vscode
.DS_Store

# Or the millions of node modules we (unfortunately) rely on
/node_modules

# No secret storage allowed too
.env
*secrets.txt

# Store none of the build assets for the installer
installers/saga-installer/
installers/InstallScripts.pkg
installers/distribution.plist
installers/Saga.pkg
installers/SagaInstaller.pkg

# Also none of the built assets
dist
installers/dist

# And none of those pesky python caches
*__pycache__